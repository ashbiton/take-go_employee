<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aviya.takeandgo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activity.LoginActivity" />
        <activity android:name=".controller.activity.AddUserActivity" />
        <activity android:name=".controller.activity.ShowCarsActivity" />
        <activity android:name=".controller.activity.MenuActivity" />
        <activity android:name=".controller.activity.AddCarModelActivity" />
        <activity android:name=".controller.activity.AddModelActivity" />
        <activity android:name=".controller.activity.ShowModelsActivity" />
        <activity android:name=".controller.activity.ShowBranchesActivity" />
        <activity android:name=".controller.activity.AddClientActivity" />
        <activity android:name=".controller.activity.ShowUsersActivity" />
        <activity android:name=".controller.activity.AddCarActivity" />
        <activity android:name=".controller.activity.DemoActivity"></activity>
    </application>

</manifest>