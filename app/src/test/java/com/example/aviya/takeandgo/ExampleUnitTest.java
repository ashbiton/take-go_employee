package com.example.aviya.takeandgo;

import android.content.ContentValues;
import android.provider.Settings;

import com.example.aviya.takeandgo.model.backend.CompanyConst;
import com.example.aviya.takeandgo.model.entities.Address;
import com.example.aviya.takeandgo.model.entities.Branch;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() throws Exception {
        /*Branch branch = new Branch();
        branch.setHouseNumber(12);
        branch.setStreet("love");
        branch.setCity("netanya");
        branch.setBranchNumber(13);
        branch.setParkingSpaces(50);

        ContentValues c1 = CompanyConst.BranchToContentValues(branch);
        Branch b1 = CompanyConst.ContentValuesToBranch(c1);

        System.out.println(b1.toString());
        System.out.println(c1.toString());*/


    }
}